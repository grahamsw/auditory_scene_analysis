## 1. Type Augmentation

- [x] 1.1 Add `Window` interface augmentation to `web/src/types.ts`

## 2. Refactoring

- [x] 2.1 Remove the `as any` cast in `web/src/audio.ts`

## 3. Verify

- [x] 3.1 Run TypeScript compiler (`tsc --noEmit`) to verify there are no type errors